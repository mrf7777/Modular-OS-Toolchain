# given a filename, create a new file that is sufficient to reconstruct
#     the original file.

# expected inputs: in_fn out_fn
main:

	# in_fn out_fn
	
	#### make a file named out_fn or clear it if it exists
	peek
	createFile
	
	# in_fn out_fn
	
	#### add the filename in_fn to the file out_fn
	
	peek
	
	# in_fn out_fn out_fn
	
	"3"
	peekBack
	
	# in_fn out_fn out_fn in_fn
	
	appendFile
	
	# in_fn out_fn
	
	#### add the filesize of in_fn to the file out_fn
	
	peek
	
	# in_fn out_fn out_fn
	
	"3"
	peekBack
	
	# in_fn out_fn out_fn in_fn
	
	getFileSize
	
	# in_fn out_fn out_fn fileSize(in_fn)
	
	appendFile
	
	# in_fn out_fn
	
	#### so input filename and filesize are recorded.
	#### recursivly append contents from file in_fn to file out_fn
	
	
copy-
